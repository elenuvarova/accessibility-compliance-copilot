[pytest]
# Discover tests in the tests/ dir. testpaths keeps pytest from wandering into
# node_modules / .venv. -p no:cacheprovider avoids writing .pytest_cache.
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra -q
